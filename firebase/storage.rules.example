rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // One enrollment JPEG per user: faces/<uid>.jpg
    match /faces/{fileName} {
      allow read, write: if request.auth != null
        && fileName == request.auth.uid + '.jpg';
    }
  }
}
